name: pHinder
# Expects the isomlab repo checked out next to this one (../isomlab).
channels:
  - conda-forge
dependencies:
  - python >=3.9
  - openpyxl
  - pip
  - pip:
      - -e ../isomlab
      - -e .
